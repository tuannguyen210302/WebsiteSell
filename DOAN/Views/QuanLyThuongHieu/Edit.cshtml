@using DOAN.Models;
@model THUONGHIEU

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
    string anhcu = ViewBag.AnhCu;
}


@section header{
    <div class="col-sm-6">
    </div>
    <div class="col-sm-6">
        <ol class="breadcrumb float-sm-right">
            <li class="breadcrumb-item"><a href="#">Trang chu</a></li>
            <li class="breadcrumb-item"><a href="@Url.Action("Index","QuanLyThuongHieu")">Thuong hieu</a></li>
            <li class="breadcrumb-item active">Chinh sua</li>
        </ol>
    </div>
}

@section script{
    <script type="text/javascript">
        function fileCheck(obj) {
            var fileExtension = ['jpeg', 'jpg', 'png', 'gif', 'bmp'];
            if ($.inArray($(obj).val().split('.').pop().toLowerCase(), fileExtension) == -1) {
                alert("Only '.jpeg','.jpg', '.png', '.gif', '.bmp' formats are allowed.");
            }
        }

        $(".custom-file-input").on("change", function () {
            var fileName = $(this).val().split("\\").pop();
            $(this).siblings(".custom-file-label").addClass("selected").html(fileName);
        });
    </script>

}

<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-1"></div>
            <!-- left column -->
            <div class="col-md-10">
                <!-- general form elements -->
                <div class="card card-primary">
                    <div class="card-header">
                        <h3 class="card-title">Chỉnh sửa</h3>
                    </div>
                    <!-- /.card-header -->
                    <!-- form start -->
                    @using (Html.BeginForm("Edit", "QuanLyThuongHieu",new { AnhCu = anhcu }, FormMethod.Post, new { enctype = "multipart/form-data" }))
                    {
                        @Html.AntiForgeryToken()

                        <div class="card-body">
                            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                            @Html.HiddenFor(model => model.IdTH)
                            <div class="form-group">

                                @Html.LabelFor(model => model.TenTH, htmlAttributes: new { @for = "inputName" })
                                @Html.TextBoxFor(model => model.TenTH, new { @type = "text", @class = "form-control", @id = "inputName", @placeholder = "Name", @autofocus = "autofocus", @readonly=true })
                                @Html.ValidationMessageFor(model => model.TenTH, "", new { @class = "text-danger" })
                            </div>

                            <div class="form-group">
                                <label for="exampleInputFile">Logo</label>
                                <div class="input-group">
                                    <div class="custom-file">
                                        @Html.LabelFor(model => model.AnhTH, "Choose File", new { @for = "AnhTH", @class = "custom-file-label" })
                                        <input class="custom-file-input" type="file" name="AnhTH" id="AnhTH" onchange="fileCheck(this);" />
                                        @Html.ValidationMessageFor(model => model.AnhTH, "", new { @class = "text-danger" })
                                    </div>
                                </div>
                            </div>

                            <div class="form-group">
                                @Html.LabelFor(model => model.SDT, htmlAttributes: new { @for = "inputName" })
                                @Html.TextBoxFor(model => model.SDT, new { @type = "text", @class = "form-control", @id = "inputName", @placeholder = "Phone", @autofocus = "autofocus" })
                                @Html.ValidationMessageFor(model => model.SDT, "", new { @class = "text-danger" })
                            </div>

                            <div class="form-group">
                                @Html.LabelFor(model => model.DiaChi, htmlAttributes: new { @for = "inputName" })
                                @Html.TextBoxFor(model => model.DiaChi, new { @type = "text", @class = "form-control", @id = "inputName", @placeholder = "Address", @autofocus = "autofocus" })
                                @Html.ValidationMessageFor(model => model.DiaChi, "", new { @class = "text-danger" })
                            </div>


                            <div class="form-group">
                                @Html.LabelFor(model => model.ChiTiet, htmlAttributes: new { @for = "inputName" })
                                @Html.TextAreaFor(model => model.ChiTiet, new { @class = "form-control", @rows = "16", @placeholder = "Enter ..." })
                            </div>
                        </div>
                        <!-- /.card-body -->
                        <div class="card-footer">
                            <button type="submit" class="btn btn-primary float-right">Lưu</button>
                        </div>
                    }
                </div>
                <!-- /.card -->
            </div>
            <!--/.col (left) -->

        </div>
        <!-- /.row -->
    </div><!-- /.container-fluid -->
</section>



